<h1><%= @wiki.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @wiki.body %></p>
  </div>
</div>

<div class="text-center">
  <% if user_signed_in? %>
    <button class="btn btn-default"><%= link_to 'Edit', edit_wiki_path(@wiki) %></button>
  <% end %>
</div>
